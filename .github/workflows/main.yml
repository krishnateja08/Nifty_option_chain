name: Nifty 50 Analysis + GitHub Pages

on:
  schedule:
    - cron: '37 3 * * 1-5'  # 9:07 AM IST - Less competition than 9:00 or 9:30
    - cron: '30 4-10 * * 1-5'  # Market hours
  workflow_dispatch:

# Required permissions
permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  analyze-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          pip install curl_cffi pandas yfinance openpyxl
      
      - name: Run Analysis
        env:
          GMAIL_USER: ${{ secrets.GMAIL_USER }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          RECIPIENT_EMAIL_1: ${{ secrets.RECIPIENT_EMAIL_1 }}
          RECIPIENT_EMAIL_2: ${{ secrets.RECIPIENT_EMAIL_2 }}
        run: |
          python nifty50_option_analysis.py
          echo "✅ Analysis complete"
          ls -la index.html
      
      # Deploy using peaceiris/actions-gh-pages (easiest method)
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: .
          publish_branch: gh-pages
          keep_files: false
          commit_message: "📊 Update Nifty Report - ${{ github.event.head_commit.message }}"
      
      - name: Show URL
        run: |
          echo "=============================================="
          echo "✅ Deployed successfully!"
          echo "=============================================="
          echo "🔗 Your report is live at:"
          echo "   https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/"
          echo "=============================================="
